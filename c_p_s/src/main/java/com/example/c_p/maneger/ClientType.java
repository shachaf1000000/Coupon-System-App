package com.example.c_p.maneger;

/**
 * ClientType defines a type of user via Enumeration, three states exist which
 * all have different Privileges to commands and resources
 */
public enum ClientType {
	ADMINISTRATOR, COMPANY, CUSTOMER
}
